# 🔧 ИСПРАВЛЕНИЕ ПОРТА 3000 → 5000

## ✅ ЧТО ИСПРАВЛЕНО:

### 1. **backend/app.js**
- ✅ Порт установлен: `const PORT = process.env.PORT || 5000;`
- ✅ Backend слушает на `0.0.0.0:5000`

### 2. **docker-compose.yml**
- ✅ Добавлено явное указание `PORT=5000` в environment секции backend
- ✅ Это переопределит любые значения из .env файла
- ✅ Проброс портов: `5000:5000`

### 3. **backend/Dockerfile**
- ✅ ENV PORT=5000
- ✅ EXPOSE 5000

## 🚀 КАК ПРИМЕНИТЬ ИСПРАВЛЕНИЯ:

### Шаг 1: Остановите все контейнеры
```bash
docker compose down
```

### Шаг 2: Пересоберите и запустите
```bash
docker compose up --build
```

### Шаг 3: Проверьте логи
```bash
docker compose logs backend
```

**Ожидаемый вывод:**
```
✅ API running on http://0.0.0.0:5000
✅ API accessible at http://localhost:5000
```

**НЕ должно быть:**
```
❌ API running on http://0.0.0.0:3000
```

## 🔍 ПРОВЕРКА:

### 1. Проверьте что порт правильный:
```bash
# В логах должно быть:
docker compose logs backend | grep "API running"
# Должно показать: ✅ API running on http://0.0.0.0:5000
```

### 2. Проверьте доступность API:
```bash
curl http://localhost:5000/api/auth/register -X POST \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456"}'
```

### 3. Проверьте в браузере:
- Откройте http://localhost:5173
- Попробуйте зарегистрироваться
- В DevTools → Network проверьте что запрос идет на `http://localhost:5000`

## ⚠️ ЕСЛИ ВСЕ ЕЩЕ ПОРТ 3000:

### Вариант 1: Проверьте .env файл
Откройте `backend/.env` и убедитесь что там нет:
```
PORT=3000
```

Если есть - удалите эту строку или измените на:
```
PORT=5000
```

### Вариант 2: Принудительно пересоберите
```bash
docker compose down -v
docker compose build --no-cache backend
docker compose up
```

### Вариант 3: Проверьте что файл сохранен
Убедитесь что изменения в `backend/app.js` сохранены:
```bash
# Проверьте содержимое файла
cat backend/app.js | grep "const PORT"
# Должно быть: const PORT = process.env.PORT || 5000;
```

## ✅ ИТОГ:

После применения исправлений:
- Backend будет слушать на порту **5000**
- Frontend будет обращаться к **http://localhost:5000**
- Все должно работать корректно

**Перезапустите проект сейчас!**

